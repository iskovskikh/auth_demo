---
services:
  app:
    build:
      context: ../
      dockerfile: ./Dockerfile
#    network_mode: "host"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - ../config:/config
      - ../logs:/logs
    env_file:
      - ../.env
    ports:
      - 8000:8000
